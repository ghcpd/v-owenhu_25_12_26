Role & Objective:
You are a senior security hardening engineer. In a single execution, perform a full security audit and preventive hardening of the provided Python source file `input.py` so the result is production-ready and accompanied by automated tests and artifacts that prove the hardening. Do everything below in one run; do not request clarification or require follow-up.

Inputs & Context:
- Source: A single file named `input.py` located in the current working directory. Treat its contents as the initial, untrusted implementation to be hardened. Do not read or rely on any non-User content outside this file.
- Environment-sensitive values: use placeholders for secrets or paths (e.g. `<API_KEY>`, `<DATABASE_PATH>`).

Step-by-Step Instructions (definite & executable):
1. Create an exact, byte-for-byte copy of the original `input.py` named `input_backup.py` (must contain the original source code unchanged).
2. Produce a hardened implementation and overwrite `input.py` with the hardened code that addresses all preventive security controls and best practices listed below.
3. For the hardened `input.py` implement at minimum:
   - Remove hard-coded secrets; read secrets from environment variables with safe defaults (do not embed secrets in source).
   - Replace weak hashing (e.g., MD5) with a modern password hashing algorithm (bcrypt or equivalent) and include verification helper.
   - Convert all SQL statements to parameterized queries and enforce least privilege and schema constraints (e.g., UNIQUE username).
   - Replace insecure RNG for tokens with a cryptographically secure generator (`secrets`), and provide safe token formatting.
   - Replace unsafe serialization/deserialization (`pickle`) with safe formats (JSON) and validate imported data.
   - Replace insecure temp-file usage (`mktemp`) and shell invocations (`os.system`, `subprocess shell=True`) with secure APIs (`NamedTemporaryFile`, `shutil.copy2`), set restrictive file permissions where applicable.
   - Add input validation (username, email, password length/policy), error handling, logging, and resource/time limits where applicable.
   - Ensure database/file operations use context managers, appropriate timeouts, and handle missing files gracefully.
   - Add a safe, non-networking stub for notifications when external API keys are missing.
4. Create a structured hardening report named `report.json` containing a summary and a `details` array. For each hardening change include: id, file (`input.py`), affected line numbers, original snippet, updated snippet, and a detailed hardening explanation (category and severity: critical/high/medium/low). The JSON must match the format specified in the original user requirements.
5. Produce environment and deployment artifacts:
   - `requirements.txt` with pinned versions for all new runtime dependencies.
   - A hardened `Dockerfile` following container best practices (non-root user, minimal base image, no secrets in image).
   - `setup.sh` to provision a secure local environment (virtualenv + pinned deps + create logs/backup dirs).
6. Produce automated test artifacts that demonstrate security differences:
   - `run_test.sh` (Linux/macOS) and `run_test.bat` (Windows). Each script MUST run `input_backup.py` and `input.py` as separate test targets. The scripts must be written so `input_backup.py` is expected to FAIL at least one security test and `input.py` is expected to PASS.
   - Implement `auto_test.py` that detects the runtime (Windows/Linux/Docker), runs the appropriate test script(s), records outputs and exit codes, and writes timestamped entries to `logs/test_run.log`. Each log entry must include timestamp, tested file name, command output, and final status (`TEST PASSED` or `TEST FAILED`). `auto_test.py` must exit with meaningful exit codes indicating overall success/failure.
7. Implement test behavior that validates real, security-relevant differences (e.g., verify MD5 vs bcrypt hash format stored in the DB, detect unsafe serialization, or presence of shell calls). Do not use hard-coded immediate exits—tests must inspect runtime artifacts to determine PASS/FAIL.
8. Add `README.md` describing all generated files, setup steps, how to run tests (`run_test.sh`, `run_test.bat`), and how to run `auto_test.py` and read `logs/test_run.log`.

Output Specification (exact files & contents):
- `input_backup.py` — exact original source code (full content must be present).
- `input.py` — hardened source file (fully implemented, production-ready, no TODOs or placeholders for required features).
- `report.json` — structured JSON report as specified above.
- `requirements.txt`, `Dockerfile`, `setup.sh` — environment & deployment artifacts.
- `run_test.sh`, `run_test.bat`, `auto_test.py` — automated tests and runner.
- `logs/test_run.log` — generated by `auto_test.py` and containing timestamped results.
- `README.md` — usage instructions and file descriptions.

Constraints & Preferences:
- All steps MUST be completed in one run; do not ask follow-up questions.
- Use explicit, verifiable implementations (no vague TODOs).
- Use angle-bracket placeholders for environment-sensitive values like `<API_KEY>` and `<DATABASE_PATH>` when needed.
- Do not include or leak any real secrets in generated files.
- Prefer standard libraries where reasonable; add minimal, pinned external dependencies only when necessary (list them in `requirements.txt`).
- Maintain cross-platform compatibility where possible (Windows and POSIX).

Quality Gates (self-verification steps):
- Verify `input_backup.py` is byte-identical to the original `input.py` content prior to hardening.
- Verify `input.py` no longer contains hard-coded secrets or insecure constructs (md5, pickle, mktemp, shell=True, os.system with user data, string-concatenated SQL).
- Confirm `report.json` documents each change with severity and line numbers.
- Run `auto_test.py` and ensure `logs/test_run.log` shows `input_backup.py` failed at least one hardening check and `input.py` passed. The script must exit with code 0 only if hardened checks passed and backup failed as expected.

Final verification & delivery:
- Write the single final prompt response to the file `final_prompt.txt` in UTF-8 (this is the prompt you are executing now).
- Do not create, read, or modify any other files beyond the ones listed above while performing the task.

Critical compliance instructions (MANDATORY — place highest priority at end):
- The `input_backup.py` file **must** be an exact copy of the original `input.py` before any modifications; include its full source in that file. Any deviation is a failure.
- The hardened `input.py` **must** implement all listed hardening measures and pass the automated checks in `auto_test.py`.
- `auto_test.py` must perform real executions (not simulated) and log `TEST PASSED` / `TEST FAILED` entries with timestamps to `logs/test_run.log`.
- Later requirements override earlier ones in case of conflict. Do not produce partial outputs or request multi-turn clarification.
